SELECT 
    p.id AS product_id,
    p.product_name,
    p.price,
    sc.sub_category_name,
    c.category_name
FROM 
    Product p
JOIN 
    Sub_Category sc ON p.sub_category_id = sc.id
JOIN 
    Category c ON sc.category_id = c.id
ORDER BY 
    c.id, sc.id, p.id;
